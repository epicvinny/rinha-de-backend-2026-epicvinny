[package]
name = "validate"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csv = "1"
ureq = { version = "2", features = ["json"] }
chrono = { version = "0.4", features = ["clock"] }
