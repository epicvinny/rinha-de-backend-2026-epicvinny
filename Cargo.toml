[workspace]
members = [
    "crates/shared",
    "crates/oracle",
    "crates/builder",
    "crates/api",
    "crates/lb",
    "crates/validate",
]
resolver = "2"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true
